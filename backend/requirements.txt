# Backend Requirements
# Python 3.11+
# 医学骨折检测分析系统 - 后端服务依赖

# ==================== Web Framework ====================
Flask==3.1.2
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-CORS==6.0.2
Flask-JWT-Extended==4.7.1
Werkzeug==3.1.4
flask-sock==0.7.0

# ==================== Cache / Queue ====================
# redis-py 必须 <6.5：kombu 5.6.2（Celery 的 broker 层）把上界钉死在 <6.5，
# 写成 8.x 会让 `pip install -r` 直接解析失败。
#
# 附带的好处：6.4.0 的 DEFAULT_RESP_VERSION = 2，而 8.x 起默认 3。
# 本机 Redis 5.0.14.1 不支持 RESP3 的 HELLO 命令，8.x 会连不上——
# 降到这里反而省掉了 core/cache.py 里 protocol=2 这个绕法（虽然那行仍保留，
# 它在两个版本下都合法）。
redis==6.4.0

# 消息队列：训练与知识库入库两类长耗时任务
celery==5.6.3

# ==================== Database ====================
SQLAlchemy==2.0.45
alembic==1.18.1
psycopg2-binary==2.9.13
pgvector==0.5.0

# ==================== YOLO Detection ====================
ultralytics==8.4.31

# ==================== Deep Learning ====================
torch==2.7.1
torchvision==0.22.1
transformers==4.57.3
accelerate==1.12.0
huggingface-hub==0.36.0

# ==================== RAG / Knowledge Base ====================
# 注意：sentence-transformers 必须 <5，6.x 起强制 transformers>=5.0，
# 会把上面钉死的 transformers==4.57.3 顶掉（ultralytics 未验证过该组合）
sentence-transformers==3.4.1
scikit-learn==1.9.1
jieba==0.42.1
rank_bm25==0.2.2
pypdf==6.19.0
# docx 入库：services/rag/loader.py 里 `from docx import Document` 是**惰性导入**
# （在 load_docx 函数内部），所以漏了它不会在启动时报错，只在真上传一份 .docx
# 时才 ImportError —— 而 pdf/md/txt 三条路径全都正常，很容易被当成"入库功能正常"。
# 本机 venv 里手工装过（Required-by 为空，不是谁的传递依赖），因此这个问题一直
# 没暴露；Docker 镜像与全新环境装完 requirements.txt 会缺它。
# 守卫：tests/unit/test_requirements_declared.py（扫全仓 import 与这份清单对账）
python-docx==1.2.0

# ==================== Image Processing ====================
opencv-python==4.12.0.88
Pillow==12.1.0

# ==================== WebSocket ====================
simple-websocket==1.1.0
wsproto==1.3.2

# ==================== Utilities ====================
numpy==2.2.6
pandas==3.0.1
scipy==1.16.3
matplotlib==3.10.8
seaborn==0.13.2
python-dateutil==2.9.0.post0
requests==2.32.5
PyYAML==6.0.3
python-dotenv==1.0.1

# ==================== Optimization ====================
optuna==4.8.0
colorlog==6.10.1

# ==================== API Clients ====================
openai==2.14.0
modelscope==1.33.0

# ==================== Dev / Test ====================
pytest==9.0.2

